#ifndef __decimal_arithmeticss__
#define __decimal_arithmeticss__

#include "decimal_num.h"

int decimal_add(decimal_t, decimal_t, decimal_t*);
int decimal_mul(decimal_t, decimal_t, decimal_t*);
int decimal_sub(decimal_t, decimal_t, decimal_t*);
int decimal_div(decimal_t, decimal_t, decimal_t*);

#endif
